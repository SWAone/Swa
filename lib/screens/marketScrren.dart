import 'package:flutter/material.dart';

class marketScrren extends StatelessWidget {
  const marketScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}